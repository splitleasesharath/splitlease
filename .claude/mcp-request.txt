TASK: Use Playwright MCP to capture a full-page screenshot of http://localhost:8000/list-with-us-v2

CONTEXT:
- The development server is confirmed running on port 8000 (HTTP 200 response)
- This is the new list-with-us-v2 page featuring the SuperDesign component
- User wants to see a visual snapshot of the page for iteration purposes

REQUIREMENTS:
1. Navigate to http://localhost:8000/list-with-us-v2
2. Wait for the page to fully load (use networkidle or similar wait strategy)
3. Capture a full-page screenshot
4. Return the screenshot so the user can see the current state of the page

PLAYWRIGHT MCP OPERATIONS NEEDED:
- playwright_navigate (to the URL)
- playwright_screenshot (full page capture)
- Proper browser context management (open and close)
