import { createRoot } from 'react-dom/client';
import ReferralLandingPage from './islands/pages/ReferralLandingPage.jsx';

createRoot(document.getElementById('referral-page')).render(<ReferralLandingPage />);
