{
  "name": "split-lease-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd app && npm install && npm run build",
    "dev": "cd app && npm run dev",
    "preview": "cd app && npm run preview"
  },
  "description": "Split Lease application monorepo"
}
